var React = require('react');

const Buttons = ({props}) => {
	return (
	<div>
		<div className="navButtons">Add a Project</div>
		<div className="navButtons">Give Feedback on a Project</div>
	</div>
	);
}

module.exports = Buttons;